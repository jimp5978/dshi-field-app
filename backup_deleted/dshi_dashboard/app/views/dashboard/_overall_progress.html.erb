<div class="section" style="flex: 1;">
    <h2 class="section-title">금월 계획 대비 전체 공정율</h2>
    <div class="section-content">
        <div class="overall-progress">
            <div class="progress-value">87%</div>
            <div class="progress-bar-container">
                <div class="progress-bar-bg">
                    <div class="progress-bar-fill" style="width: 87%"></div>
                </div>
            </div>
            <div class="progress-details">
                <div class="progress-detail">
                    <span class="detail-label">계획 수량:</span>
                    <span class="detail-value">1,250</span>
                </div>
                <div class="progress-detail">
                    <span class="detail-label">완료 수량:</span>
                    <span class="detail-value">1,088</span>
                </div>
                <div class="progress-detail">
                    <span class="detail-label">잔여 수량:</span>
                    <span class="detail-value">162</span>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .overall-progress {
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        height: 100%;
        gap: clamp(8px, 1.5vh, 12px);
    }
    
    .progress-value {
        font-size: clamp(32px, 8vh, 48px);
        font-weight: bold;
        color: #00d4ff;
    }
    
    .progress-bar-container {
        width: 100%;
        margin: clamp(8px, 1.5vh, 12px) 0;
    }
    
    .progress-bar-bg {
        background-color: #555555;
        height: clamp(8px, 1.5vh, 12px);
        border-radius: clamp(4px, 0.8vh, 6px);
        overflow: hidden;
        position: relative;
    }
    
    .progress-bar-fill {
        height: 100%;
        background: linear-gradient(90deg, #00d4ff, #2196F3);
        transition: width 0.5s ease;
        border-radius: clamp(4px, 0.8vh, 6px);
    }
    
    .progress-details {
        display: flex;
        flex-direction: column;
        gap: clamp(4px, 0.8vh, 6px);
        width: 100%;
    }
    
    .progress-detail {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: clamp(4px, 0.8vh, 6px) 0;
        border-bottom: 1px solid #555555;
    }
    
    .detail-label {
        color: #cccccc;
        font-size: clamp(10px, 2vh, 14px);
    }
    
    .detail-value {
        color: #ffffff;
        font-size: clamp(10px, 2vh, 14px);
        font-weight: bold;
    }
</style>