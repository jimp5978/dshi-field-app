<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DSHI 생산 관리 대시보드</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <style>
        :root {
            --header-height: clamp(50px, 8vh, 80px);
            --filter-height: clamp(40px, 6vh, 60px);
            --content-height: calc(100vh - var(--header-height) - var(--filter-height) - 20px);
        }
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background-color: #2a2a2a;
            color: #ffffff;
            padding: 5px;
            line-height: 1.2;
            min-width: 768px;
            height: 100vh;
            overflow: hidden;
            position: fixed;
            width: 100%;
            top: 0;
            left: 0;
        }
        
        .dashboard-container {
            max-width: 1800px;
            margin: 0 auto;
            width: 100%;
            height: 100%;
            display: flex;
            flex-direction: column;
        }
        
        .header {
            background-color: #3a3a3a;
            padding: clamp(8px, 2vh, 15px) clamp(15px, 3vw, 25px);
            border-radius: clamp(4px, 1vh, 8px);
            margin-bottom: clamp(6px, 1.5vh, 10px);
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-left: clamp(3px, 0.6vw, 6px) solid #00d4ff;
            flex-shrink: 0;
            height: var(--header-height);
        }
        
        .header h1 {
            color: #ffffff;
            font-size: clamp(14px, 3.5vh, 22px);
            font-weight: 600;
        }
        
        .header .update-time {
            color: #999999;
            font-size: clamp(10px, 2.5vh, 16px);
        }
        
        .filter-section {
            background-color: #3a3a3a;
            padding: clamp(8px, 2vh, 15px) clamp(15px, 3vw, 25px);
            border-radius: clamp(4px, 1vh, 8px);
            margin-bottom: clamp(6px, 1.5vh, 10px);
            display: flex;
            align-items: center;
            gap: clamp(8px, 2vw, 16px);
            flex-shrink: 0;
            height: var(--filter-height);
        }
        
        .filter-label {
            color: #cccccc;
            font-weight: 500;
            font-size: clamp(10px, 2.2vh, 15px);
        }
        
        .filter-buttons {
            display: flex;
            gap: clamp(3px, 0.8vw, 8px);
        }
        
        .filter-btn {
            background-color: #4a4a4a;
            color: #ffffff;
            border: 1px solid #666;
            padding: clamp(4px, 1vh, 8px) clamp(8px, 2vw, 16px);
            border-radius: clamp(3px, 0.6vh, 6px);
            cursor: pointer;
            font-size: clamp(10px, 2.2vh, 15px);
            font-weight: 500;
            transition: all 0.2s;
            min-height: clamp(28px, 6vh, 40px);
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .filter-btn:hover {
            background-color: #00d4ff;
            color: #2a2a2a;
        }
        
        .filter-btn.active {
            background-color: #00d4ff;
            color: #2a2a2a;
        }
        
        .content {
            flex: 1;
            overflow: hidden;
            height: var(--content-height);
        }
        
        .dashboard-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: clamp(8px, 1.5vh, 12px);
            height: 100%;
        }
        
        .left-column, .right-column {
            display: flex;
            flex-direction: column;
            gap: clamp(8px, 1.5vh, 12px);
            height: 100%;
        }
        
        .section {
            background-color: #3a3a3a;
            border-radius: clamp(4px, 1vh, 8px);
            padding: clamp(12px, 2.5vh, 20px);
            display: flex;
            flex-direction: column;
            border-left: clamp(3px, 0.6vw, 6px) solid #00d4ff;
            overflow: hidden;
        }
        
        .section-title {
            color: #ffffff;
            font-size: clamp(12px, 2.8vh, 18px);
            font-weight: 600;
            margin-bottom: clamp(10px, 2vh, 16px);
            display: flex;
            align-items: center;
            gap: clamp(8px, 1.5vw, 12px);
        }
        
        .section-content {
            flex: 1;
            overflow: hidden;
        }
    </style>
</head>
<body>
    <div class="dashboard-container">
        <div class="header">
            <h1>DSHI 생산 관리 대시보드</h1>
            <div class="update-time">마지막 업데이트: <%= Time.current.strftime("%Y-%m-%d %H:%M:%S") %></div>
        </div>
        
        <div class="filter-section" data-controller="dashboard">
            <div class="filter-label">필터:</div>
            <div class="filter-buttons">
                <button class="filter-btn active" data-filter="all" data-action="click->dashboard#filter" data-dashboard-target="filterBtn">전체</button>
                <button class="filter-btn" data-filter="priority-1" data-action="click->dashboard#filter" data-dashboard-target="filterBtn">우선순위 1</button>
                <button class="filter-btn" data-filter="priority-2" data-action="click->dashboard#filter" data-dashboard-target="filterBtn">우선순위 2</button>
                <button class="filter-btn" data-filter="priority-3" data-action="click->dashboard#filter" data-dashboard-target="filterBtn">우선순위 3</button>
            </div>
        </div>
        
        <div class="content">
            <%= yield %>
        </div>
    </div>
    
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
</body>
</html>
